interface:
  display_name: "Tighten Docs"
  short_description: "Default quality pass for clear current docs"
  default_prompt: "Use $tighten-docs from first draft through final review to make this settled documentation direct and current, and remove rejected decisions instead of preserving them as negation."
