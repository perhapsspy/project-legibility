interface:
  display_name: "Agents MD Editor"
  short_description: "Keep always-read instructions short and durable"
  default_prompt: "Use $agents-md-editor to keep the lasting rules here and move or remove everything that should not load every time."
