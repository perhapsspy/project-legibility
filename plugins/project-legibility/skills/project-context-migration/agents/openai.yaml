interface:
  display_name: "Project Context Migration"
  short_description: "Turn scattered notes into resumable project context"
  default_prompt: "Use $project-context-migration to sort these scattered notes and keep only what future work needs."
