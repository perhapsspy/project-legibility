interface:
  display_name: "Interactive State Flow"
  short_description: "Keep interactions responsive and stale results out"
  default_prompt: "Use $interactive-state-flow to make this interaction respond promptly and ignore results that no longer belong."
