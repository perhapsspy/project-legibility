interface:
  display_name: "Interactive State Flow"
  short_description: "Fix laggy or stale interactive flows"
  default_prompt: "Use $interactive-state-flow to refactor this laggy or stale interaction so source state, deferred presentation, and async freshness are handled clearly."
