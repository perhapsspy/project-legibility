interface:
  display_name: "Project Context"
  short_description: "Resume long-running work without rebuilding context"
  default_prompt: "Use $project-context to record where this work stands and what the next task should do."
