interface:
  display_name: "Project Context"
  short_description: "Keep long-running repo work resumable"
  default_prompt: "Use $project-context to create or update durable context for this long-running repository task."

