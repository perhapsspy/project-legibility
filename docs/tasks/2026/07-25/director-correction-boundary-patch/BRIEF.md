# 디렉터 교정 경계 패치

## 목표

- 기존 포함 스킬 `codex-project-director`의 사용자 교정 인계 경계를 정본 커밋 `9988e92897325da83225297a54d4449f8d137594`로 갱신하고 Project Legibility 패치 버전으로 배포한다.

## 범위

- 디렉터 출처 잠금·생성 묶음, 관련 선택·비선택 회귀, 플러그인 버전과 한영 변경 기록을 갱신한다.
- 정상 출시와 배포 목록 고정, 로컬 플러그인 갱신까지 확인한다.
- 다른 포함 스킬의 정본·역할·호출 계약은 유지한다.

## 현재 사실

- 정본 변경은 결함 증거와 제안 해법의 분리, 승인 계약값의 정확한 인계, 사용자 교정과 충돌하는 영향 작업의 중단·지시 철회·담당자 확인 후 재개를 다룬다.
- 정본 `main`과 원격 `main`은 `9988e92897325da83225297a54d4449f8d137594`로 일치한다. 이 커밋은 교정 인계 규칙과 기존 `NEEDS_DECISION` 경계를 함께 보존한다.
- 제품 구성에서 `codex-project-director`는 사용자가 명시한 여러 Codex 작업의 디렉터 역할에만 선택된다.

## 현재 상태

- Project Legibility `0.6.1` 후보는 디렉터 출처·생성 묶음, 선택·비선택 회귀, 패치 버전과 한영 변경 기록을 포함하며 전체 로컬 검증이 통과했다.

## 다음 행동

- 공개 원격 재현 검증 뒤 후보를 커밋하고 지속 통합을 확인한다.

## 작업 경계

- `plugins/project-legibility/`
- `tests/routing/`
- `CHANGELOG*.md`
- `docs/tasks/2026/07-25/director-correction-boundary-patch/`
