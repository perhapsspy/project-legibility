**2026-08-14**
- Director의 명시 호출·선택 범위와 제품 역할은 유지되고, 선택된 뒤의 실행 소유권과 failure handling만 교정된다.
- Project Legibility 0.8.1 patch로 배포하고 Luna는 재현 사례로만 취급한다.
- 기존 공개 선택 계약을 넓히거나 좁히지 않는 호환 가능한 guardrail 교정이기 때문이다.
- runtime 계약·changelog·회귀 판정은 특정 모델명이 아니라 role·model·tool·worker provisioning 실패 전체를 기준으로 한다.
